Host lists for push_files.ksh and rotate_ssh_key.sh, one hostname per line, # comments allowed.

Only `hosts.example` is committed. Real lists (`<env>.hosts`) contain internal hostnames and are
gitignored; keep them here on the admin host, or point PUSH_HOSTFILE in vios.conf at them.
